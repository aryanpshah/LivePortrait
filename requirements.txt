-r requirements_base.txt
# Consolidated requirements for LivePortrait
# Recommended: create and activate a virtual environment before installing
# GPU vs CPU: the repo previously used onnxruntime-gpu. If you do NOT have an NVIDIA GPU
# or do not want GPU acceleration, replace `onnxruntime-gpu` with `onnxruntime`.

# Base dependencies (expanded from requirements_base.txt)
numpy==1.26.4
mediapipe==0.10.9
pyyaml==6.0.1
opencv-python==4.10.0.84
scipy==1.13.1
imageio==2.34.2
lmdb==1.4.1
tqdm==4.66.4
rich==13.7.1
ffmpeg-python==0.2.0
onnx==1.16.1
scikit-image==0.24.0
albumentations==1.4.2
matplotlib==3.9.0
imageio-ffmpeg==0.5.1
tyro==0.8.5
gradio==5.1.0
pykalman==0.9.7
pillow>=10.2.0
requests==2.32.3

# Extras used by the project
# onnxruntime-gpu==1.18.0
transformers==4.38.0
torch==2.3.1
torchvision==0.18.1

# Optional: if you prefer CPU-only ONNX runtime, comment the GPU line above and
# uncomment the following:
onnxruntime==1.18.0

# To install:
# python -m pip install -U pip
# python -m pip install -r requirements.txt
