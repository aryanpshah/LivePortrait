-r requirements_base.txt

# PyTorch (LivePortrait wrapper, asymmetry_transfer, keypoint_map, etc.)
torch==2.3.0
torchvision==0.18.0

# Used by bundled inference / ONNX paths
transformers==4.38.0
onnxruntime==1.18.0

# Face mesh for mouth TPS (legacy mp.solutions.face_mesh; removed in mediapipe 0.10.30+)
mediapipe>=0.10.14,<0.10.30

# Install from this folder:
#   python -m pip install -U pip
#   python -m pip install -r requirements.txt
#
# GPU PyTorch: install torch/torchvision from pytorch.org for your CUDA version,
# then install the rest from this file (skip torch lines or pin matching versions).
